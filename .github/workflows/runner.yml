name: Runner
run-name: Runner ${{ github.run_number }}

on:
  schedule:
    - cron: "*/40 * * * *"

  workflow_dispatch:

jobs:
  runner:
    runs-on: ubuntu-latest
    container: ghcr.io/opencidn/cidn/runner:v0.0.96
    steps:
      - shell: sh
        run: runner --handler-name=cidn-${{ github.run_number }} --update-duration=10s --duration=4h --concurrency=2 ${{ secrets.ARGS_CIDN }}
