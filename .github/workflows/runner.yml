name: Runner
run-name: Runner ${{ github.run_number }}

on:
  schedule:
    - cron: "*/30 * * * *"

  workflow_dispatch:

jobs:
  runner:
    runs-on: ubuntu-latest
    container: ghcr.io/opencidn/cidn/runner:v0.0.64
    steps:
      - shell: sh
        run: runner --handler-name=${{ github.run_number }} --update-duration=10s --concurrency=8 ${{ secrets.ARGS_CIDN }}
